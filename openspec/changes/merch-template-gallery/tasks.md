## 1. Webflow (already live)

- [x] 1.1 Add combo `is-default` on the in-page merch `.product-gallery-collection` (not the fullscreen collection)
- [x] 1.2 Embed `js/product.js` in the Merch template footer at the same pin as product; do not add it on `/merch` listing

## 2. README

- [x] 2.1 In `README.md`, set `js/product.js` to product and merch detail, and `js/merch.js` to merch listing only
