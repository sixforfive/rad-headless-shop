## 1. CSS

- [x] 1.1 In `css/global.css`, add `border-color: #fff` on `body.is-full-screen` and `body.is-full-screen *`; keep existing `color: #fff` on `body.is-full-screen` only

## 2. Check

- [x] 2.1 Open the product gallery: borders are `#fff`. Close it: borders return to theme tokens. Repeat on merch detail.
