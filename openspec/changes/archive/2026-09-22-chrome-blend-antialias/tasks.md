## 1. Chrome CSS

- [x] 1.1 In `css/global.css`, replace the `.navbar, .second-men, .layer.is-blend` block: set `-webkit-font-smoothing: antialiased` and `-moz-osx-font-smoothing: grayscale` on `.navbar`, `.second-menu`, `.second-menu .text-meta`, `.footer`, `.footer-link`, and `.layer.is-blend`; set `mix-blend-mode: difference` on `.navbar`, `.second-menu`, and `.layer.is-blend` only
