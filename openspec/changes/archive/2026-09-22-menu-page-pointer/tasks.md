## 1. Pointer JS

- [x] 1.1 In `js/global.js`, map pathname first segment to `#pointer-collection` `/`, `#pointer-shop` `/shop`, `#pointer-merch` `/merch`, `#pointer-faq` `/faq`
- [x] 1.2 Toggle `is-none` on `.menu-drawer .menu-list .menu-pointer`: drop it on the match, keep it on the rest; unmapped paths keep every pointer hidden

## 2. Docs

- [x] 2.1 In `README.md`, note the pointer toggle on the `js/global.js` row
- [x] 2.2 In `.cursor/rules/webflow-rad.mdc`, list the four pointer ids on the `drawer-wrapper` row
- [ ] 2.3 Point the Footer `js/global.js` tag at the new commit SHA and publish
