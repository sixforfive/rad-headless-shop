## 1. CSS

- [x] 1.1 In `css/global.css`, rename `.layer.is-blend #add-to-cart` to `.layer.is-blend #add-to-cart-landscape`
- [x] 1.2 In `css/global.css`, delete `body.is-full-screen .layer-cta .qty-dropdown` and `body.is-full-screen .layer-cta .qty-dropdown *` from the white-chrome rule, and delete `body.is-full-screen .layer-cta .qty-dropdown { mix-blend-mode: difference }`

## 2. Retire

- [x] 2.1 Delete `openspec/changes/qty-dropdown-fullscreen-blend/`
